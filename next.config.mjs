/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  trailingSlash: true,
  images: {
    // Next.js image optimization (Vercel default). Add remote patterns here if needed.
    remotePatterns: [],
  },
};

export default nextConfig;
