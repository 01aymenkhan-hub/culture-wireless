/**
 * PostCSS config for Tailwind CSS v4.
 * Tailwind v4 ships as a PostCSS plugin under @tailwindcss/postcss.
 */
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
